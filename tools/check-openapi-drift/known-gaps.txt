# Known OpenAPI ↔ router drift, accepted as a baseline so that
# CI fails only on NEW drift introduced by future changes.
#
# Format:
#   `+ /path`  — registered in router.go but NOT in openapi.yaml
#   `- /path`  — declared in openapi.yaml but NOT in router.go
#
# These entries SHOULD be reduced over time as part of the v1.0 spec-sync
# work (see roadmap "Documentation & release"). When you remove an entry,
# either delete the route or add a matching path to openapi.yaml.

# ---- Spec/docs endpoints — they ARE the documentation, no need to self-document ----
+ /docs
+ /openapi.yaml

# ---- /api/v1/cabinets/* — legacy alias of /api/v1/seller-cabinets/*. Routes to the
# same handlers; intentionally undocumented to discourage new clients from binding to
# the alias. Remove from the router (and from this file) once no caller uses it. ----
+ /api/v1/cabinets
+ /api/v1/cabinets/{id}
+ /api/v1/cabinets/{id}/campaigns
+ /api/v1/cabinets/{id}/communication/reputation
+ /api/v1/cabinets/{id}/products
+ /api/v1/cabinets/{id}/sync

# ---- Compatibility/debug endpoints. Kept out of public OpenAPI until the
# v1.0 spec-sync pass decides whether they are public product API or internal
# diagnostics. ----
+ /api/v1/ads/data-health
+ /api/v1/ads/debug/normquery
+ /api/v1/campaigns/{id}/bids/min
+ /api/v1/extension/token/exchange
+ /api/v1/workspaces/{workspaceId}/ads-intelligence/wb/data-health
+ /api/v1/workspaces/{workspaceId}/ads-intelligence/wb/debug/normquery

# ---- Repricer (WB price management). Full OpenAPI schemas are pending the v1.0
# spec-sync pass; routes are live and workspace-scoped. All price fields are
# integer rubles. ----
+ /api/v1/prices
+ /api/v1/prices/sync
+ /api/v1/prices/bulk
+ /api/v1/prices/quarantine
+ /api/v1/price-changes
+ /api/v1/price-changes/{changeId}/rollback
+ /api/v1/price-upload-tasks
+ /api/v1/price-schedules
+ /api/v1/price-schedules/{scheduleId}
+ /api/v1/repricer/run
+ /api/v1/prices/cabinets-status
