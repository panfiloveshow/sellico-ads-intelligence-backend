// PLACEHOLDER — generated file, run `pnpm openapi:generate` to populate.
// The real file is produced by openapi-typescript from ../../openapi/openapi.yaml.
// Until then, the typed surface is empty so the project builds; calls go
// through openapi-fetch as plain fetch + URL strings.

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface paths {}
export interface components {}
export type webhooks = Record<string, never>;
export type operations = Record<string, never>;
export type external = Record<string, never>;
