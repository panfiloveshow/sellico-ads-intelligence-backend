node_modules
dist
*.local
.vite

# tsc emit when noEmit is bypassed (build script uses --noEmit, but defence in depth)
*.tsbuildinfo
src/**/*.js
src/**/*.d.ts
!src/vite-env.d.ts
vite.config.js
vite.config.d.ts

# Generated OpenAPI client — checked into commits because regenerated less
# often than imports change; remove this line if you prefer codegen-on-build.
# src/api/schema.gen.ts
